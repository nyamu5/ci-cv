# TICKET 3.4 — Landing Page & Dynamic OG Images

**Stage:** 3 — User Interface
**Goal:** The homepage and shareable preview images.

```
Build the landing page and OG image generation:

1. app/page.tsx — landing page:
   - Hero: "Get brutally honest feedback on your CV" + subheadline
   - The CvInputForm directly on the page
   - Four feature cards (section scoring, JD matching, red flags, rewrites)
   - A "How it works" three-step section

2. app/analysis/[id]/opengraph-image.tsx — dynamic OG image via
   next/og ImageResponse:
   - Fetch the analysis by id
   - 1200x630, dark slate background
   - "CV Roaster 🔥" wordmark, large colour-coded score, "Overall CV
     Score" label, up to 3 red-flag pills, "cv-roaster" footer
   - Inline styles only (ImageResponse does not support Tailwind)
   - NOTE: do NOT set runtime = 'edge' — this deploys on Railway (Node.js)
   - Export size and alt per the next/og API

3. Add openGraph and twitter metadata in app/analysis/[id]/page.tsx
   referencing the generated image.

4. Footer component: "Built by Nyamu Wanyoike · github.com/nyamu5 ·
   linkedin.com/in/nyamu5"
```

**Verification:**

```
- pnpm typecheck passes
- pnpm build succeeds
- Visiting /analysis/{id}/opengraph-image renders a PNG with the score
- Pasting an analysis URL into opengraph.xyz shows the dynamic card
```

**Commit:**

```
git add -A && git commit -m "feat: landing page and dynamic OG images"
```
