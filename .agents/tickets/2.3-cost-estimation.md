# TICKET 2.3 — Cost Estimation Helper + Tests

**Stage:** 2 — Core Engine (PDF + Rate Limit + AI Pipeline)
**Goal:** Accurate per-request AI cost calculation.

```
Build the cost tracking helper:

1. lib/costs.ts — export estimateCost(inputTokens, outputTokens, model):
   Pricing per 1M tokens:
     gpt-4o-mini: input $0.15, output $0.60
     gpt-4o:      input $2.50, output $10.00
   Return the cost in USD as a number rounded to 6 decimal places.
   Throw on an unknown model string.

2. tests/costs.test.ts — Vitest tests:
   - gpt-4o-mini with 1000 in / 500 out tokens returns the expected cost
   - gpt-4o with 1000 in / 500 out tokens returns the expected cost
   - An unknown model throws
   - Zero tokens returns 0
```

**Verification:**

```
- pnpm typecheck passes
- pnpm test passes — cost tests green
```

**Commit:**

```
git add -A && git commit -m "feat: AI cost estimation helper with tests"
```
