# TICKET 0.2 — Environment & Folder Structure

**Stage:** 0 — Repository Initialisation
**Goal:** Complete folder skeleton and environment variable template.

```
Create the full project skeleton for cv-roaster:

1. Create .env.example with all variable names and placeholder comments:
   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   SUPABASE_SERVICE_ROLE_KEY=
   # OpenAI (traced via LangSmith)
   OPENAI_API_KEY=
   LANGSMITH_API_KEY=
   LANGSMITH_PROJECT=cv-roaster
   # Upstash Redis
   UPSTASH_REDIS_REST_URL=
   UPSTASH_REDIS_REST_TOKEN=
   # Trigger.dev
   TRIGGER_SECRET_KEY=
   # Database (Supabase direct connection)
   DATABASE_URL=

2. Copy .env.example to .env.local (user fills in real values manually).

3. Create the folder structure with placeholder files (each file should have
   a // TODO comment and minimal valid exports so typecheck passes):

   app/analysis/[id]/page.tsx
   app/analysis/[id]/opengraph-image.tsx
   app/dashboard/page.tsx
   app/login/page.tsx
   app/auth/callback/route.ts
   app/api/analyse/route.ts
   app/api/analysis/[id]/route.ts
   app/api/upload/route.ts
   app/api/trigger/route.ts

   components/cv-input-form.tsx
   components/analysis-result.tsx
   components/analysis-pending.tsx
   components/jd-match-card.tsx
   components/score-card.tsx
   components/red-flags-list.tsx
   components/rewrite-suggestions.tsx
   components/share-button.tsx
   components/nav.tsx
   components/auth-button.tsx

   lib/supabase/client.ts
   lib/supabase/server.ts
   lib/db/schema.ts
   lib/db/index.ts
   lib/openai.ts
   lib/pdf.ts
   lib/rate-limit.ts
   lib/costs.ts
   lib/validations.ts

   trigger/run-analysis.ts

   tests/validations.test.ts
   tests/costs.test.ts
   tests/pdf.test.ts

4. Create a lib/env.ts module that validates required env vars at startup
   using Zod — throws a clear error if any required variable is missing.

5. Create drizzle.config.ts pointing at lib/db/schema.ts and using DATABASE_URL.
```

**Verification:**

```
- pnpm typecheck passes (all placeholder files compile)
- All folders and files listed above exist
- lib/env.ts throws a descriptive error if a var is missing (test by
  temporarily renaming .env.local)
```

**Commit:**

```
git add -A && git commit -m "chore: scaffold folder structure and env validation"
```
